\section{Deformation gradient and motion}
\label{subsec:deformgradmotion}
\begin{sectionref}
The references used in this section are:
    \begin{itemize}
        \item Belytschko \cite{Belytschko_Liu_Moran_Elkhodary_2014}
        \begin{itemize}
            \item 3.2.2-3.2.3
            \item 3.2.6
            \item 3.7.1
        \end{itemize}
        \item Continuum Mechanics website \cite{McGinty}
        \begin{itemize}
            \item Deformation Gradient
            \item Polar Decomposition
        \end{itemize}
    \end{itemize}
\end{sectionref}

\subsection{Introduction}
\marginpar{\footnotesize This subsection has relied heavily on \cite{McGinty}. Most of the notes are a literal copy of the reference. Especially for the explanation of the rotation and stretch tensor}
Continuum mechanics introduce the concepts of \textbf{material} and \textbf{spatial} definition. See \cref{fig:spatialmaterialdescription}. The difference between material and spatial is the definition of what is observed:
\begin{itemize}
    \begin{keyconcept}[unbreakable]{Material vs Spatial description}
        \label{keyconcept:materialspatial}
            \item Material $\rightarrow$ Lagrangian: Following a person in a crowd. Typical in solid mechanics (FEA).
            \item Spatial $\rightarrow$ Eulerian: Fixing a point in a crowd and seeing who passes through. Typical in fluid mechanics (CFD).
    \end{keyconcept}
\end{itemize}

The configuration of a body is defined by the symbol $\Omega$ and it's boundaries are represented by $\Gamma$. The subscript $_0$ indicates an initial state. To go from $\Omega_0$ to $\Omega$, the body goes from $\mathbf{X}$ to $\mathbf{x}$. Essentially $\mathbf{X}$ means which particle (where is it initially) and $\mathbf{x}$ means where is it now? The idea is the same for the definition of any point $\mathbf{P}$ and $\mathbf{p}$. Then, how $\mathbf{x}$ relates to $\mathbf{X}$ is defined by the deformation map $\varphi(\mathbf{X},t)$ and it's displacement $\mathbf{u}=\mathbf{x} - \mathbf{X}$. The deformation map can be seen as a tracker in the "crowd" example above: Given this initial point in space, what happend to whatever went through this point initially.

\input{block04/DeformationGradient/figures/spatialmaterialdescription.tex}[H]


\subsection{Deformation Gradient}
The deformation gradient $\mathbf{F}$ is simply the derivative of each component of the deformed vector from the deformed configuration:  $\mathbf{x}$ with respect to each component of the reference configuration $\mathbf{X}$:
\begin{equation}
    \label{eq:deformationgradient}
    F_{ij}=
    x_{i,j}=
    \dfrac{\partial x_{i}}{\partial X_j}
    = \begin{bmatrix}
        \dfrac{\partial x_{1}}{\partial X_1} && \dfrac{\partial x_{1}}{\partial X_2 } && \dfrac{\partial x_{1}}{\partial X_3} \\
        \dfrac{\partial x_{2}}{\partial X_1} && \dfrac{\partial x_{2}}{\partial X_2 } && \dfrac{\partial x_{2}}{\partial X_3} \\
        \dfrac{\partial x_{3}}{\partial X_1} && \dfrac{\partial x_{3}}{\partial X_2 } && \dfrac{\partial x_{3}}{\partial X_3}
    \end{bmatrix}
\end{equation}

\begin{example}{Rigid body motion}
    Given the following mapping:
$$ 
    \mathbf{u} = \begin{bmatrix}
        x = X + 5 \\
        y = Y + 2
    \end{bmatrix}
$$
Find the deformation gradient.\\
\textbf{Answer: } Here, $\mathbf{F}$ is simply $\mathbf{I}$ because
$$
    \mathbf{F}=\dfrac{x_i}{X_j}=\begin{bmatrix}
        \dfrac{\partial (X + 5)}{\partial X} && \dfrac{\partial (X + 5)}{\partial Y}\\
        \dfrac{\partial (Y + 2)}{\partial X} && \dfrac{\partial (Y + 2)}{\partial Y}
    \end{bmatrix}
    =
    \begin{bmatrix}
        1 && 0 \\
        0 && 1
    \end{bmatrix}
$$
Thus, the body is not deformed hence the title \it{Rigid body}.
\end{example}

\begin{example}{Rotation}
    Given the following mapping:
$$ 
    \mathbf{u} = \begin{bmatrix}
        x = X \cos \theta - Y \sin \theta \\
        y = X \sin \theta + Y \cos \theta
    \end{bmatrix}
$$
Find the deformation gradient.\\
\textbf{Answer: }
$\mathbf{F}$ is then:
$$
    \mathbf{F}=\dfrac{\partial x_i}{\partial X_j}=\begin{bmatrix}
        \dfrac{\partial (X \cos \theta - Y \sin \theta)}{\partial X} && \dfrac{\partial (X \cos \theta - Y \sin \theta)}{\partial Y}\\
        \dfrac{\partial (X \sin \theta + Y \cos \theta)}{\partial X} && \dfrac{\partial (X \sin \theta + Y \cos \theta)}{\partial Y}
    \end{bmatrix}
$$ 
$$
    \mathbf{F}=
    \begin{bmatrix}
        \cos \theta && -\sin \theta \\
        \sin \theta && \cos \theta
    \end{bmatrix}
$$
\end{example}
\begin{keyconcept}{Deformation gradient and hourglassing}
    Sometimes, displaced bodies that undergo true deformation do not yield deformation when computed. This is the case with hourglassing, a numerical error in finite element when elements do not yield any deformation when in fact they deform. It is typical in dominant bending problems and when selected elements use reduced integration. While this is out of topic, it is an important concept and gives a good example of the calculation of deformation gradient. Example adapted and taken from \cite{McGinty}.

    The displacement function is given by:
    $$
        \mathbf{u} = \begin{bmatrix}
            X + \tfrac{1}{4}XY + 5 \\
            Y + 4
        \end{bmatrix}
    $$
    The deformation gradient is then:
    $$
        \mathbf{F} = \begin{bmatrix}
            1+\tfrac{1}{4}Y && \tfrac{1}{4}X \\
            0 && 1
        \end{bmatrix}
    $$
    at $(0,0)$, the initial position of the centre of the square, the deformation gradient is $\mathbf{I}$ since $X=0$ and $Y=0$, even though visually the body deforms:
    $$
        \mathbf{F} = \begin{bmatrix}
            1 && 0 \\
            0 && 1
        \end{bmatrix}
    $$
    
    \input{block04/DeformationGradient/figures/deformationgradient_hourglass.tex}
\end{keyconcept}

The determinant of the deformation gradient is called the jacobian determinant:
\begin{equation}
    \mathbf{J = \det F}    
\end{equation}
It represents the volume ratio of a body:
$$  
    \begin{cases}
        J = 1 & \text{Incompressible}   \\
        J > 1 & \text{Expanding volume} \\
        J < 1 & \text{Compressed volume}
    \end{cases}
$$
It must obey:
\begin{equation}
    \mathbf{J > 0}
\end{equation}
Because $\mathbf{F}$: \begin{enumerate}
    \item Must have an inverse
    \item Would mean negative volume
\end{enumerate} 

\subsection{Polar Decomposition}
The subject of rotation has been approached in a previous example, but this subject is rather important in solid mechanics. Indeed, each deformed body can be explained in parts by translation (strech) and rotation. In large displacement solid mechanics, rotation do not generate strain. Thus, the deformation gradient can be obtained by:

\begin{enumerate}
    \item Rotating first then stretch, which gives:
    \begin{equation}
        \mathbf{F = R \cdot U}
        \label{eq:deformationstretch}
    \end{equation}
    \item Stretching first then rotate, which gives:
    \begin{equation}
        \mathbf{F = V \cdot R}
        \label{eq:deformationrotation}
    \end{equation}
\end{enumerate}



Here $\mathbf{U}$ and $\mathbf{V}$ are the right strech tensor and left stretch tensor respectively. $\mathbf{R}$ is the rotation matrix. From \cref{eq:deformationstretch}  and \cref{eq:deformationrotation} we can confidently say that:
\begin{equation}
    \mathbf{V \cdot R = R \cdot U}
\end{equation}

\begin{keyconcept}{Interesting properties of the rotation matrix $R$}
    The followings are two interesting properties of the rotation matrix:
    \begin{equation}
        \det{R} = 1
    \end{equation}
    \begin{equation}
        R^{-1} = R^T \ \rightarrow \text{Orthogonal property}
    \end{equation}
 
\end{keyconcept}

by multiplying both sides by $\mathbf{R^T}$:
\begin{equation}
    \mathbf{V \cdot R \cdot R^T = R \cdot U \cdot R^T}
\end{equation}

\begin{equation}
    \mathbf{V \cdot I = R \cdot U \cdot R^T}
\end{equation}

Thus, 

\begin{equation}
    \mathbf{V = R \cdot U \cdot R^T}
\end{equation}

and,

\begin{equation}
    \mathbf{U = R^T \cdot V \cdot R}
\end{equation}

Knowing the interesting properties above, the partitioning of stretch and rotation can be determined using:
\begin{equation}
    \mathbf{F^T \cdot F = ( R\cdot U )^T \cdot ( R\cdot U )^T}=\mathbf{U^T \cdot R^T \cdot R \cdot U}
\end{equation}
The same goes with the left stretch tensor ($\mathbf{V}$) thus
\begin{equation}
    \label{eq:gradienttransposeproperty}
    \mathbf{F^T \cdot F = U^T \cdot U = V^T\cdot V}
\end{equation}

\begin{example}{Stretching and Rotation of a body}
    Given the following coordinate mapping:
    $$
        \begin{bmatrix}
            x \\ y
        \end{bmatrix}
        = \begin{bmatrix}
            1.30X - 0.375Y \\
            0.750X + 0.650Y
        \end{bmatrix}
    $$
    Find the deformation gradient and partition the rotation and stretch tensor.
    
    \textbf{Answer: } Use the right stretch tensor $\mathbf{U}$ to find $\mathbf{R}$. First, the deformation gradient:
    $$
        \mathbf{F}=\dfrac{x_i}{X_j}=\begin{bmatrix}
            \dfrac{\partial (1.30X - 0.375Y)}{\partial X} && \dfrac{\partial (1.30X - 0.375Y)}{\partial Y}\\
            \dfrac{\partial (0.750X + 0.650Y)}{\partial X} && \dfrac{\partial (0.750X + 0.650Y)}{\partial Y}
        \end{bmatrix}
    $$
    $$
        \mathbf{F}=\begin{bmatrix}
            1.300 && -0.375 \\
            0.750 && 0.650
        \end{bmatrix}
    $$
    Hence, $\mathbf{F^T \cdot F = U^T \cdot U }$. Thus we find $\mathbf{U}$ from taking the square root of $\mathbf{F^T \cdot F}$:
    $$
        \mathbf{U = \sqrt{U^T \cdot U}}
    $$
    $$
        \mathbf{U} = \sqrt{\begin{bmatrix}
            2.500 && 0.000 \\
            0.000 && 0.536
        \end{bmatrix}
        } = \begin{bmatrix}
            1.500 && 0.000 \\
            0.000 && 0.75
        \end{bmatrix}
    $$
    Then we multiply $\mathbf{F}$ by $\mathbf{U^{-1}}$ to obtain $\mathbf{R}$
    $$
        \mathbf{F \cdot U^{-1} = R \cdot U \cdot U^{-1} = R}
    $$
    where 
    $$
        U^{-1} = \begin{bmatrix}
            0.667 && 0.000 \\
            0.000 && 1.333
        \end{bmatrix}
    $$
    thus,
    $$
        \mathbf{R = F \cdot U^{-1} = \begin{bmatrix}
            1.300 && -0.375 \\
            0.750 && 0.650
        \end{bmatrix} 
        \begin{bmatrix}
            0.667 && 0.000 \\
            0.000 && 1.333
        \end{bmatrix}}
    $$
    $$
        R =
        \begin{bmatrix}
            0.866 && -0.500 \\
            0.500 && 0.866
        \end{bmatrix}
    $$
    \input{block04/DeformationGradient/figures/stretchandrotation.tex}    
\end{example}
Note that other methods to find $\mathbf{R}$ exist, to list a few:
\begin{enumerate}
    \item Using the left stretch tensor $\mathbf{F = V R}$
    \item Iterative procedure:
    $$
        A_{n+1} = \tfrac{1}{2}(A_n + A_n^{-T})
    $$
    where $\mathbf{A_n}$ is replaced by $\mathbf{F}$. However, the transpose and inverse of $\mathbf{F}$ have to be computed to find $F_n^{-T}$. After a few iterations, this yields $\mathbf{R}$. Then, $\mathbf{V}$ or $\mathbf{U}$ can be found by using the extensions of \cref{eq:deformationstretch} and \cref{eq:deformationrotation}, remember $\mathbf{R^T = R^{-1}}$:
    $$
        \mathbf{R^{-1} F = R^{-1} R U}
    $$
    and 
    $$
        \mathbf{F R^{-1} = V R R^{-1}}
    $$
    yields:
    \begin{equation}
        \mathbf{U = R^T F}
    \end{equation}
    and:
    \begin{equation}
        \mathbf{V = F R^T}
    \end{equation}
\end{enumerate}

\input{block04/DeformationGradient/problemset.tex}














